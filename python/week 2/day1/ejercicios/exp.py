# 1
class Cat:
    def __init__(self, name, age):
        self.name = name
        self.age = age


def older_cat(cats):
    if cat >= cats:
        print('you are older than')

        print(cat.age)
    # def older_cat(self):
    #     if age > :
    #     return age
    #     else:
    #     return age
    # print("the oldest cat is: " + self.age + " years old")


cat1 = Cat("mary", 20)

cat2 = Cat("molly", 30)

cat3 = Cat("kiki", 50)

cats_list = [cat1, cat2, cat3]

older_cat(cats_list)
